import { noindexMetadata } from "@/lib/seo";

export const metadata = noindexMetadata;

export default function LaunchChecklistLayout({ children }: { children: React.ReactNode }) {
  return children;
}
